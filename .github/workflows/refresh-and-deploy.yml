name: Refresh Settings & Deploy

on:
  schedule:
    # Run daily at 06:00 UTC
    - cron: '0 6 * * *'
  workflow_dispatch:
    # Allow manual trigger

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  refresh-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Reuse Next.js compiler/bundler cache across runs (not the page output).
      - name: Cache Next.js build
        uses: actions/cache@v4
        with:
          path: .next/cache
          key: nextjs-${{ hashFiles('package-lock.json') }}-${{ hashFiles('src/**', 'next.config.*') }}
          restore-keys: |
            nextjs-${{ hashFiles('package-lock.json') }}-

      # ── Fetch fresh data from Microsoft Graph ──
      - name: Fetch settings from Intune
        env:
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
        run: npx tsx scripts/fetch-settings.ts

      # ── Fetch Microsoft security baseline templates (after fetch-settings,
      #    so setting names resolve against the fresh catalog) ──
      - name: Fetch security baselines
        env:
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
        run: npx tsx scripts/fetch-baselines.ts

      # ── Generate changelog by diffing against previous snapshot ──
      - name: Generate changelog
        run: npx tsx scripts/generate-changelog.ts

      # ── AI summaries for new changelog days (no-op when nothing new).
      #    continue-on-error: summaries are garnish — never block a deploy if
      #    the key is missing or the API is down; the card just hides. ──
      - name: Generate AI summaries
        continue-on-error: true
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          npx tsx scripts/generate-summaries.ts --all
          # Monthly roundup for newly completed months (no-op otherwise).
          npx tsx scripts/generate-summaries.ts --all-months

      # ── Build search index and category tree ──
      - name: Build search index
        run: npx tsx scripts/build-search-index.ts

      # ── Pre-generate per-setting JSON for changelog row expand ──
      - name: Generate per-setting changelog JSON
        run: npx tsx scripts/generate-changelog-settings.ts

      # ── Commit updated data files back to repo ──
      - name: Commit data updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f \
            data/settings.json \
            data/categories.json \
            data/category-tree.json \
            data/last-updated.json \
            data/changelog.json \
            data/settings-previous.json \
            data/categories-previous.json \
            public/search-index.json \
            public/oma-uri-index.json \
            public/baselines
          # Summaries file may not exist if the AI step was skipped/failed.
          git add -f data/changelog-summaries.json 2>/dev/null || true
          git diff --cached --quiet || git commit -m "chore: update settings data $(date -I)"
          git push

      # ── Build the Next.js static site ──
      - name: Determine base path
        id: basepath
        run: |
          if [ -f public/CNAME ]; then
            echo "value=" >> "$GITHUB_OUTPUT"
          else
            echo "value=/${{ github.event.repository.name }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build site
        env:
          NEXT_BASE_PATH: ${{ steps.basepath.outputs.value }}
        run: npm run build

      # ── Deploy to GitHub Pages ──
      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
